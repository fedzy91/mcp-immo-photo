services:
  - type: web
      name: mcp-immo-photo
          runtime: docker
              plan: free
                  envVars:
                        - key: PORT
                                value: 8000
